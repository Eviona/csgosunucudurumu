var config = {};

config.true = "true" // Controls the arrangement of the message. More information in the docs.

config.color = "random" // Only Hex color / Example : #FFFF00 (Thats yellow...) / More colors in this site https://htmlcolorcodes.com

config.token = "TOKEN" // Bot token example in the docs.

config.id = "740819050921459712" // Message id. More information in the docs.

config.channelid = "514005831877132288" // Channel ID. More information in the docs

config.servers = 
    [{
        name: "[TR] SonJeton JailBreak Sunucusu", // Something like "Surf Combat" "Retake"
        ip: "45.10.56.63", // Server IP. No Port. No Domain. Only numbers!!!
        port: 27015 // Server port. after the ip theres : this is the port. (You should know that...)
        }]


module.exports = config
